HELP_TEXTS = {
    "filter_type": """
### 🎨 Сглаживание (filter_type)
Первичная обработка изображения перед сегментацией. Влияет на качество и скорость обработки.

| Метод | Суть | Когда использовать |
|---|---|---|
| **pyrMeanShift** | Ищет области с похожим цветом и текстурой, заливает одним тоном. Делает фото похожим на рисунок красками. | Аниме, мультяшный стиль |
| **bilateral** | «Умное» размытие: убирает шум, но сохраняет резкость контуров объектов. | Портреты, реалистичный стиль |
| **median** | Простое медианное усреднение пикселей. Хорошо убивает мелкий «солевой» шум. | Фото с сильным шумом |
| **none** | Без предобработки. | Чистые изображения |

**⚠️ Совет:** pyrMeanShift + Watershed часто дают лучший результат для мультяшного стиля.
Bilateral хорошо работает с Weighted K-Means для портретов.
""",

    "saliency_map": """
### 🧠 Карта внимания (Saliency Map)
**Математика:** использует Fine-Grained Saliency Detection для анализа контраста и контуров уже сглаженного (отфильтрованного) изображения. Создает тепловую карту от 0 до 1.

**Суть:** алгоритм находит «главный объект» на фото. При постобработке он автоматически делает мазки краски на объекте мелкими и детальными, а на фоне — крупными и упрощенными.
Формула: `local_threshold = base_threshold * (bg_scale - mean_saliency * (bg_scale - focus_scale))`

✅ Рекомендуется для портретов и предметной съемки.
""",

    "spatial_method": """
### 🗺️ Метод пространственной сегментации (spatial_method)
Определяет, как изображение разбивается на зоны **до** подбора цветов. Критически влияет на стиль результата.

**SLIC (Superpixels)**
Кластеризация в 5D-пространстве (L, a, b, X, Y) с использованием алгоритма Simple Linear Iterative Clustering.
Разбивает картинку на «соты» (мазки кистью). Идеально для художественного эффекта.
- `region_size` — размер одного мазка в пикселях (10–20 для деталей, 30–50 для упрощения)
- `ruler` — квадратичность/гибкость формы мазка (выше = ровнее, 10–20 обычно)

**Watershed (Водораздел)**
Использует морфологический градиент и маркеры для сегментации.
Считает картинку топографической картой, где светлые края — горы.
«Затапливает» долины цветом, пока вода не упрётся в горы.
Создаёт органические пятна, идеально облегающие реальные контуры.
Идеален для мультяшного и векторного стиля.

**none** — без пространственной сегментации. Использует все пиксели напрямую.

⚠️ **Конфликты:** 
- Watershed + auto_color может быть медленным на больших изображениях
- SLIC + hierarchical_split часто избыточны вместе
""",

    "palette_method": """
### 🎨 Метод подбора палитры (palette_method)
Как алгоритм выбирает N красок из миллионов цветов фотографии. Определяет качество и реалистичность результата.

**K-Means**
Минимизирует дисперсию внутри кластеров (WCSS) через итеративный алгоритм Lloyd.
Ищет «среднее арифметическое» масс цветов.
⚠️ Может потерять мелкие детали (например, чёрный зрачок на белом фоне).
✅ Быстро работает, хорошо для пейзажей.

**Weighted K-Means**
Находит контуры объектов через детектор Canny и дублирует их в массиве данных ×weight_factor.
Заставляет K-Means уделить особое внимание тонким линиям и контурам.
✅ Идеально для портретов и объектов с чёткими границами.

**Median Cut / Octree**
Разделяет куб цветового пространства RGB на подкубы по медиане рекурсивно.
**Гарантированно сохраняет экстремальные цвета.**
Если на фото есть хоть 10 чёрных пикселей глаз — алгоритм выделит для них баночку краски.
✅ Идеально для портретов и изображений с важными деталями.

⚠️ **Совет:** Median Cut медленнее K-Means, но лучше сохраняет детали.
""",

    "auto_color": """
### 🔢 Авто-цвет (Auto Color / Elbow Method)
Автоматически определяет оптимальное количество красок без ручного подбора.

**Алгоритм:**
1. Тестирует изображение при K от `k_min` до `k_max` с шагом `k_step` 
2. Для каждого K вычисляет WCSS (сумму квадратов расстояний до центроидов)
3. Находит «локоть» кривой — точку, после которой добавление новых красок перестаёт улучшать детализацию
4. Возвращает найденное оптимальное K

**Параметры:**
- `k_min`, `k_max` — диапазон поиска (обычно 5–30)
- `k_step` — шаг поиска (2–5 для баланса скорости и точности)
- `subsample_ratio` — доля пикселей для быстрого расчёта (0.2 = 20% изображения)

✅ Рекомендуется для пейзажей и сложных сцен.
⚠️ Может быть медленным на больших изображениях. Используй `subsample_ratio < 0.3`.
⚠️ Конфликтует с ручным указанием `colours_cnt` (игнорируется).
""",

    "hierarchical_split": """
### 🌲 Иерархическое расщепление (Hierarchical Split)
Находит большие «скучные» монотонные пятна (например, градиентное небо)
и только внутри них проводит дополнительное дробление на 2–3 оттенка.

**Зачем:** спасает от потери деталей в больших однородных областях
без фрагментации уже детализированных объектов.

**Алгоритм:**
1. Находит связные компоненты в карте кластеров
2. Для каждой компоненты вычисляет дисперсию цветов
3. Если компонента крупная И скучная (variance > threshold), запускает локальный K-Means
4. Добавляет новые центры в общую палитру

**Параметры:**
- `min_area_pixels` — минимальный размер пятна (в пикселях) для расщепления (1000–5000)
- `variance_threshold` — порог дисперсии цвета (10–50), выше которого пятно считается «интересным»

⚠️ **Совет:** Используй с `palette_method=kmeans` для лучшего результата.
⚠️ Может быть медленным на изображениях с большими однородными областями.
""",

    "grow_details": """
### 🔬 Выращивание деталей (Grow Details)
**Математика:** бинарная морфологическая дилатация с использованием структурирующего элемента.

**Суть:** если мелкая деталь (зрачок, блик, веснушка) оказалась меньше минимального
размера кисточки, алгоритм не удаляет её, а физически «раздувает» во все стороны
за счёт соседних пикселей, пока область не станет достаточно большой для раскрашивания.

**Параметры:**
- `max_dilation_steps` — максимальное количество итераций расширения (1–10)

✅ Идеально для портретов (спасает зрачки и веснушки).
⚠️ Может сделать тонкие линии толще. Используй с осторожностью.
⚠️ Несовместима с `spatial_method=none` (требует маски компонент).
""",

    "weighted_kmeans": """
### ⚖️ Взвешенный K-Means (Weighted K-Means)
Модификация стандартного K-Means, которая принудительно фокусируется на контурах объектов.

**Алгоритм:**
1. Применяет детектор Canny для нахождения краёв
2. Дублирует пиксели на краях в обучающем массиве в `weight_factor` раз (по умолчанию ×5)
3. K-Means «видит» контурные пиксели как более значимые и гарантированно выделяет для них цвет

**Параметры:**
- `edge_threshold1`, `edge_threshold2` — пороги детектора Canny (обычно 50–150)
- `weight_factor` — кратность усиления контурных пикселей (3–10)

✅ Идеально для портретов и объектов с чёткими границами.
✅ Хорошо работает с `palette_method=kmeans` и `filter_type=bilateral`.
⚠️ Может быть медленным на больших изображениях.
""",
}


def get_help(key: str) -> str:
    """
    Возвращает справочный текст для параметра.
    
    Args:
        key: Название параметра
    
    Returns:
        Markdown-строка с объяснением или пустая строка, если параметр не найден
    """
    return HELP_TEXTS.get(key, "")
